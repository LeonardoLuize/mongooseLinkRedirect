<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Links | All</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800&display=swap" rel="stylesheet">
</head>
<body>
    <style>
        *{
            font-family: 'Montserrat', sans-serif;
        }

        body{
            background-color: #2A2B2A;
            padding-bottom: 50px;
        }

        #container{
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }

        h1{
            color: #C6DDF0;
        }

        a{
            text-decoration: none;
        }

        h2{
            margin-top: 0px;
            color: #C99DA3;
        }
        
        p{
            color: #996888;
        }

        .aSquare{
            color: #996888;
            font-weight: bold;
            transition-property: color;
            transition-duration: .3s;
        }

        .aAdd{
            color: #996888;
            font-weight: bold;
            transition-property: color;
            transition-duration: .3s;
        }

        .aUrl{
            color: #996888;
            transition-property: color;    
            transition-duration: .3s;        
        }

        .aUrl:hover{
            color: #C99DA3;
        }
        

        .aAdd:hover{
            color: #C99DA3;
        }


        .aSquare:hover{
            color: #C6DDF0;
        }

        button{
            background-color: transparent;
            color: #C6DDF0;
            border-radius: 20px;
            padding: 5px 10px 5px 10px;
            margin-right: 10px;
            cursor: pointer;
            border: solid 2px #C6DDF0;
            font-size: 16px;
            transition-property: background-color color;
            transition-duration: .3s;
            font-weight: bold;
        }

        button:hover{
            background-color: #C6DDF0;
            color: #996888;
        }

        .square{
            padding: 15px 20px 15px 20px;
            border-radius: 20px;
            margin-top: 50px;
            background-color: #5E4955;
        }
    </style>
    <div id="container">
        <h1>Links</h1>
        <a class="aAdd" href="/add">Adicionar Link</a>

        <%for(let i = 0; i < links.length; i++) {%>
            <form method="POST" action="/<%=links[i]._id%>?_method=DELETE">
                <div class="square" id="<%=links[i]._id%>">
                    <h2><%=links[i].title%></h2>
                    <p><%=links[i].description%></p>
                    <a class="aUrl" target="new" href="/<%=links[i].title%>"><%=links[i].url%></a>
                    <p>Clicks: <%=links[i].click%></p>
                    <button type="submit" onclick="deleteLink('<%=links[i]._id%>')">Excluir</button>
                    <a class="aSquare" href="/edit/<%=links[i]._id%>">Editar link</a>
                </div>
            </form>
        <%}%>    

    </div>


</body>
</html>